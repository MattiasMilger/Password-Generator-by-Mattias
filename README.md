This application is built entirely using standard Python libraries available in the Python Standard Library.
Requirements
You must have Python 3.x installed on your system, as well as the following libraries.

tkinter (for the Graphical User Interface)

secrets (for generating cryptographically strong random numbers)

string (for character set constants)

How to Run the App
Save the Code: Save the entire Python code provided and run it.
